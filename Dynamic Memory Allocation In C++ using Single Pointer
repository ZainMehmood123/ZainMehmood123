#include<iostream>
using namespace std;
int main()
{
	
	int size;
	int *p;
		p = &size;
	cout << "Enter the szie = ";
	cin >> size;
	p = new int[size];
	for (int i = 0; i < size; i++)
	{
		cin >> p[i];
	}
	for (int j = 0; j < size; j++)
	{
		cout << p[j];
	}
	system("pause");
	return 0;
}
